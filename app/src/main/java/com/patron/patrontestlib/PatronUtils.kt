package com.patron.patrontestlib

import android.content.Context
import com.google.android.flexbox.FlexboxLayout

class PatronUtils {
    fun evaluate(context: Context) {
        FlexboxLayout(context)
    }
}